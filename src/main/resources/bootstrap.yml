spring:
    application:
        name: content
    profiles:
        active: ${profileActive}
    #cloud:
    #    config:
    #        uri: ${configUrl}
    #        profile: ${profileActive}
    #        label: master
    #        fail-fast: true
